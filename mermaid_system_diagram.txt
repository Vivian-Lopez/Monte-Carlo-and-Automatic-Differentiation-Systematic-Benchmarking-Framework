classDiagram
    direction TB

    class WorkloadConfig {
        <<abstract>>
        +workload_type: str
        +M: int
        +seed: int
        +validate()
        +to_dict() dict
        +from_dict(data) WorkloadConfig
        +config_hash() str
    }

    class EuropeanOptionConfig {
        +workload_type = "european"
        +S0: float = 100.0
        +K: float = 100.0
        +r: float = 0.05
        +sigma: float = 0.20
        +T: float = 1.0
        +option_type: str = "call"
        +N: int = 1
        +M: int = 10000
        +seed: int = 42
        +validate()
        +to_dict() dict
        +from_dict(data) EuropeanOptionConfig
    }

    class MonteCarloEngine {
        <<abstract>>
        +run(config, ad_mode) (float, dict|None)
        +supports(workload_type) bool
        +supported_ad_modes() tuple
    }

    class CPUMonteCarloEngine {
        +supported_ad_modes() = ("none",)
        +run(config, ad_mode) (float, None)
        -_price_european(config) float
    }

    class JAXMonteCarloEngine {
        +supported_ad_modes() = ("none","forward","reverse")
        +run(config, ad_mode) (float, dict|None)
        -_run_european(config, ad_mode)
        -_compute_greeks(kernel, config, ad_mode)
    }

    class CUDAMonteCarloEngine {
        +supported_ad_modes() = ("none","forward")
        +run(config, ad_mode) (float, dict|None)
        -_KERNEL_SOURCE: cuRAND C++ kernel
    }

    class CPPMonteCarloEngine {
        +supported_ad_modes() = ("none",)
        +run(config, ad_mode) (float, None)
        -price_european: pybind11 → OpenMP C++
    }

    class BenchmarkRunner {
        +engine: MonteCarloEngine
        +name: str
        +run(config, ad_mode, num_warmup, num_runs) BenchmarkResult
        +capture_environment() dict
        -_run_with_timings(config, num_warmup, num_runs, ad_mode)
    }

    class BenchmarkResult {
        +config: WorkloadConfig
        +result: float
        +runtimes: List[float]
        +mean_runtime: float
        +std_runtime: float
        +min_runtime: float
        +max_runtime: float
        +config_hash: str
        +metadata: dict
        +ad_mode: str
        +ad_overhead_ratio: float
        +gradient_time_ms: float
        +baseline_mean_ms: float
        +memory_peak_mb: float
        +ad_accuracy_error: float
        +throughput_paths_per_sec: float
        +greeks: dict|None
        +from_runs(...)$ BenchmarkResult
    }

    class BenchmarkDB {
        -db_path: results/benchmarks.db
        -_lock: threading.Lock
        +create_run(config_dict, engine, ad_mode) str
        +mark_running(run_id)
        +mark_completed(run_id, result_value, mean_runtime_ms, std_runtime_ms, ad_overhead_ratio, greeks)
        +mark_failed(run_id, error_message)
        +store_run_full(config_dict, engine, ad_mode, ...) str
        +get_run(run_id) dict
        +get_all_runs(limit, workload_type, engine, status) list
        +get_pending_runs() list
        +summary() dict
    }

    class RunsTable {
        <<SQLite Table: runs>>
        -- Identity / Grouping --
        +id: TEXT PK
        +experiment_id: TEXT
        +experiment_type: TEXT
        +config_hash: TEXT
        -- Status Lifecycle --
        +status: TEXT NOT NULL DEFAULT 'pending'
        +error_message: TEXT
        +created_at: TEXT NOT NULL
        +started_at: TEXT
        +completed_at: TEXT
        -- Workload Definition --
        +workload_type: TEXT NOT NULL
        +M: INTEGER
        +N: INTEGER
        +seed: INTEGER
        +config_json: TEXT NOT NULL
        -- Engine / Implementation --
        +engine: TEXT NOT NULL
        +language: TEXT
        +backend: TEXT
        -- Parallelism / Scalability --
        +num_threads: INTEGER
        +vectorization_flag: TEXT
        +batch_size: INTEGER
        -- Runtime / Performance --
        +mean_runtime_ms: REAL
        +std_runtime_ms: REAL
        +min_runtime_ms: REAL
        +max_runtime_ms: REAL
        +throughput_paths_per_sec: REAL
        -- AD Metadata --
        +ad_mode: TEXT NOT NULL DEFAULT 'none'
        +baseline_mean_ms: REAL
        +ad_overhead_ratio: REAL
        -- Numerical Results --
        +result_value: REAL
        +greek_delta: REAL
        +greek_vega: REAL
        +greek_rho: REAL
        -- Analytical Reference --
        +analytical_price: REAL
        +analytical_delta: REAL
        +analytical_vega: REAL
        +analytical_rho: REAL
        -- Error Metrics --
        +abs_price_error: REAL
        +rel_price_error: REAL
        +abs_delta_error: REAL
        +rel_delta_error: REAL
        +abs_vega_error: REAL
        +rel_vega_error: REAL
        +abs_rho_error: REAL
        +rel_rho_error: REAL
        -- Resource Utilisation --
        +memory_peak_mb: REAL
        -- Environment / Hardware --
        +cpu_model: TEXT
        +cpu_architecture: TEXT
        +cpu_count: INTEGER
        +memory_gb: REAL
        +platform: TEXT
        +python_version: TEXT
        +numpy_version: TEXT
        +jax_version: TEXT
        +blas_backend: TEXT
        -- Cloud Fields --
        +cloud_provider: TEXT
        +instance_type: TEXT
        +cost_per_run: REAL
        -- Legacy --
        +greeks_json: TEXT
        -- Indexes --
        +idx_status ON status
        +idx_workload ON workload_type
        +idx_engine ON engine
        +idx_experiment ON experiment_id
    }

    class FlaskAPI {
        +POST /api/runs
        +GET  /api/runs
        +GET  /api/runs/:id
        +GET  /api/engines
        +GET  /api/workloads
        +GET  /api/summary
        +GET  /api/compare_matrix
        -_worker_loop() daemon thread
    }

    class ReactFrontend {
        +SimulatePage
        +ComparePage
        +HistoryPage
        +SummaryPage
        +AnalysisPage
        +api/client.ts
    }

    WorkloadConfig <|-- EuropeanOptionConfig
    MonteCarloEngine <|-- CPUMonteCarloEngine
    MonteCarloEngine <|-- JAXMonteCarloEngine
    MonteCarloEngine <|-- CUDAMonteCarloEngine
    MonteCarloEngine <|-- CPPMonteCarloEngine

    BenchmarkRunner o-- MonteCarloEngine : uses
    BenchmarkRunner ..> BenchmarkResult : produces
    BenchmarkResult o-- WorkloadConfig : contains

    FlaskAPI --> BenchmarkRunner : dispatches via worker thread
    FlaskAPI --> BenchmarkDB : reads / queries
    BenchmarkResult --> BenchmarkDB : persisted by API worker

    BenchmarkDB --> RunsTable : reads/writes via SQLite

    ReactFrontend --> FlaskAPI : HTTP + JSON