[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "rust_mc"
version = "0.1.0"
description = "Rayon-parallelised Rust Monte Carlo pricer for local volatility (PyO3)"
requires-python = ">=3.9"
