# Core numerical stack
numpy>=1.26
scipy>=1.11

# AD / JAX engine (CPU)
jax[cpu]>=0.4.28

# API backend
flask>=3.0
flask-cors>=4.0

# Testing
pytest>=8.0

# Process/thread diagnostics
psutil>=5.9

# Optional C++ build support (only needed if building benchmarking/cpp)
pybind11>=2.11